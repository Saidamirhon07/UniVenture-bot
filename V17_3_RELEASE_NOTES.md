# UniVentureAI V17.3 — In-Chat App Launcher

V17.3 keeps the complete V17.1 behavior and makes one focused `/start` improvement.

- The welcome message now contains one full-width **🚀 Open UniVentureAI** button directly beneath it.
- The button opens the authenticated Mini App immediately.
- The existing bot menu, Free Check, SAT, IELTS and Premium shortcuts remain in the code and continue working where shown.
- The V17 Plan redesign and V17.1 payment handoff fix are included.
- No Railway variables, database changes or `/data` migration are required.

Python compilation, launcher contract tests, payment delivery tests, backend regression tests and the production frontend build pass. Native Telegram appearance still requires one production-device check after deployment.
